/*
const producto = {
    nombre: 'Nombre',
    precio: 30,
    disponible: true,
}

const medidas = {
  peso: '1kg',
  medida: '1m',
}

console.log(producto);
console.log(medidas);

const resultado = Object.assign(producto, medidas); // uno 2 objetos.

// Spread Operator o Rest Operator
const resultado2 = { ...producto, ...medidas }
// ...producto se lee: toma una copia de producto y asignado al objeto resultado2.

console.log(resultado);
console.log(resultado2);
*/