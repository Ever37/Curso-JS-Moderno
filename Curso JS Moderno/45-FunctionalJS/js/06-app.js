// Funciones PURAS
// Retornan un dato, pero no modifican los valores de las variables
// toman un parametro y retoman lo mismo.

/*
const numero1 = 20;
const duplicar = numero => numero * 2;
const resul = duplicar(numero1);
console.log(resul);
*/