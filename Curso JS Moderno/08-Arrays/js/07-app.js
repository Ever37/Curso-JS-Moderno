/*
const carrito = [];

// Definir un producto
const producto = {
    nombre: 'Monitor 32 Pulgadas',
    precio: 400
}

const producto2 = {
    nombre: 'Celular',
    precio: 800
}

const producto3 = {
    nombre: 'Teclado',
    precio: 50
}

const producto4 = {
    nombre: 'Parlante',
    precio: 500
}

// Forma imperativa
carrito.push(producto); //Agregar al final
carrito.push(producto2);
carrito.push(producto4);
carrito.unshift(producto3); //Agrega al inicio

console.table(carrito);

// Eliminar último elemento de un arreglo
carrito.pop()
console.table(carrito);

// Eliminar primer elemento de un arreglo
carrito.shift()
console.table(carrito);


// param: primer elemento posición inicial
// param: segundo elemento, cantidad elem a eliminar
carrito.splice(1,1)
console.table(carrito);
*/